sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t sebastianulbrich/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sebastianulbrich/docker-react npm run test -- --coverage